<%= form_with(model: catch, local: true) do |form| %>
  <% if catch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(catch.errors.count, "error") %> prohibited this catch from being saved:</h2>

      <ul>
      <% catch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :length %>
    <%= form.number_field :length, id: :catch_length %>
  </div>

  <div class="field">
    <%= form.label :time %>
    <%= form.datetime_select :time, id: :catch_time %>
  </div>

  <div class="field">
    <%= form.label :notes %>
    <%= form.text_area :notes, id: :catch_notes %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :catch_user_id %>
  </div>

  <div class="field">
    <%= form.label :fish_id %>
    <%= form.text_field :fish_id, id: :catch_fish_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
